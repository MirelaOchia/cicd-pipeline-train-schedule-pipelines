pipeline {
  agent any
  stages {
    stage ('Build') {
      steps {
        echo 'Running build automation'
        sh 'cp /tmp/mirela/source/file_to_move /tmp/mirela/destination/file_to_move'
      }
    }
  }
}
